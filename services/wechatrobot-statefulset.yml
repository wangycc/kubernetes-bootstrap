apiVersion: apps/v1
kind: StatefulSet
metadata:
  name:  wechatrobot
spec:
  selector:
    matchLabels:
      app: wechatrobot 
  serviceName: "wechatrobot"
  podManagementPolicy: "Parallel"
  replicas: 1
  template:
    metadata:
      labels:
        app: wechatrobot
    spec:
      terminationGracePeriodSeconds: 10
      nodeSelector:
        kubernetes.io/hostname: 10.111.209.190
      containers:
      - name: wechatrobot
        image: hub.pri.ibanyu.com/service/wechatrobot:latest-test
        imagePullPolicy: Always
        args:
          - -serv
          - base/wechatrobot
          - --skey
          - "$(MY_POD_NAME)"
        env:
          - name: MY_POD_NAME
            valueFrom:
              fieldRef:
                fieldPath: metadata.name
        volumeMounts:
        - name: log-dir
          mountPath: /data/servicelog/wread0
      volumes:
      - name: log-dir
        hostPath:
          path: /data/servicelog/wread0
          type: Directory