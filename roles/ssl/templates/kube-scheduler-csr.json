{
    "CN": "system:kube-scheduler",
    "hosts": [
      "127.0.0.1",
     {% for ip in groups['masters'] %}
       {%- if not loop.last -%}
   "{{ ip }}",
       {% else -%}
   "{{ ip }}"
       {% endif %}
     {% endfor %}
    ],
    "key": {
        "algo": "rsa",
        "size": 2048
    },
    "names": [
      {
        "C": "CN",
        "ST": "BeiJing",
        "L": "BeiJing",
        "O": "system:kube-scheduler",
        "OU": "system"
      }
    ]
}
